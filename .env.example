# Optional — Google Apps Script web-app URL for cross-device state persistence.
# If unset, the app falls back to localStorage only.
# See google-apps-script/Code.gs for deployment instructions.
VITE_SHEETS_URL=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec
